import 'package:flutter/material.dart';

class SurveysPage extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Surveys Page'),
      ),
    );
  }
}